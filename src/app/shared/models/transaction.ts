export interface Transaction {
  amount: string;
  created: string;
  currency: string;
  destination: string;
  number: string;
  paid_amount: string;
  source: string;
  status: string;
  type: string;
}
